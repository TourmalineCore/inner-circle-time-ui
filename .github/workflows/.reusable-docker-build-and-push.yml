name: Publish Docker image

on:
  # to allow to wait for a docker image to be published to proceed in another workflow
  workflow_call:

jobs:
  push-to-registry:
    uses: TourmalineCore/inner-circle-github-workflows/.github/workflows/docker_build_and_push.yml@feature/update-workflows
    # without this it cannot login to the registry
    secrets: inherit